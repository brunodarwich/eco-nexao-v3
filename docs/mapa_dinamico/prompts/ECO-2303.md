# Prompt — ECO-2303: contrato visual do mapa v2

```text
Execute somente ECO-2303 — contrato e payload visual do mapa v2, sem UI.

Você é coordenador. Siga docs/mapa_dinamico/protocolo_sessoes.md. Crie e encerre em
sequência: planejador → implementador → testador → revisor → consolidador. Mantenha no
máximo um subagente ativo e proíba netos.

Dependência: ECO-2302 deve estar comprovada. Preserve alterações alheias e registre
baseline Git/estado do worktree.

Objetivo observável:
Fazer `GET /routes/{route_id}/map` devolver metadados visuais suficientes para que web
e nativo não codifiquem cores: pin com category_slug/label/color/icon e payload com
legenda/contagens. Esta task não cria camadas espaciais Rota/Cidade.

Leia AGENTS, spec §§7.2/8.4, OpenAPI, ADR 0003, ADR 0010, AC-MAP, playbook, schemas,
service/repository e tipos gerados. Trabalhe contract-first.

Arquivos permitidos:
- docs/openapi.yaml;
- schemas/envelopes, serviço/repositório estritamente necessários ao payload;
- testes OpenAPI/API/service;
- tipos TypeScript gerados e aliases do contrato;
- docs da task.

Proibido:
- editar MapAdapter, tela de mapa ou filtros;
- incluir regra `route_corridor`/`citywide` ainda não aceita;
- fallback silencioso para cores fixas;
- quebrar consumidores sem estratégia de transição;
- refatorar APIs não relacionadas.

Aceite/testes:
- pin e legenda têm os mesmos metadados normalizados;
- ausência editorial de cor/ícone tem comportamento contratual explícito, não inventado;
- contagens são coerentes com pins retornados;
- OpenAPI check/generate sem drift, pytest focal, typecheck;
- compatibilidade e tamanho do payload revisados.

Não implemente a UI e não inicie ECO-2304.
```
