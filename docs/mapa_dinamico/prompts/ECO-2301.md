# Prompt — ECO-2301: decisão de taxonomia visual

```text
Execute somente ECO-2301 — decisão de taxonomia visual do mapa ECOnexão.

Você é o coordenador. Leia integralmente e obedeça
docs/mapa_dinamico/protocolo_sessoes.md. Crie e encerre, sempre um por vez, os
subagentes: planejador → implementador documental → testador documental → revisor →
consolidador. Nenhum subagente pode criar outros. Não edite antes do planejador.

Objetivo observável:
Produzir uma proposta documental para aprovação humana contendo categorias públicas,
labels, slugs, cores, ícones, ordem, regra de `outros` oculto e separação de
`emergência` em `saúde` e `segurança`. Não alterar código, OpenAPI, banco ou importer.

Leia: AGENTS.md; docs/README.md; backend_integration_spec.md; ai_task_playbook.md;
data/pindobal_data_contract.md; acceptance_criteria.md; design-specs/DESIGN.md;
mapa_dinamico/README.md e plano_implementacao.md. Inspecione a taxonomia no importer,
actor_categories, API e adapters apenas em leitura.

Permitido:
- criar proposta `docs/adr/0010-taxonomia-visual-do-mapa.md` com status `proposto`;
- atualizar documentos deste pacote para registrar a proposta e perguntas ao owner;
- produzir tabela de mapeamento categoria atual → proposta.

Proibido:
- marcar ADR como aceito;
- escolher silenciosamente cor, ícone, slug ou migração de dados;
- editar código, migration, OpenAPI, mock ou design de tela;
- reclassificar dados.

Aceite da sessão:
- proposta compara manter `emergência` versus separar saúde/segurança;
- cor nunca é o único identificador e contraste/acessibilidade são considerados;
- lista conflitos e impacto sobre dados existentes;
- owner recebe perguntas binárias/objetivas e consequências;
- sessão termina `BLOCKED` aguardando owner se não houver aprovação explícita.

Validação: links Markdown, `rg` de slugs atuais/propostos, coerência com contrato
Pindobal e revisão de que nenhum arquivo de produto foi alterado.

Entregue no formato obrigatório do protocolo e não inicie ECO-2302.
```
