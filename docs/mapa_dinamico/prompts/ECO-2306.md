# Prompt — ECO-2306: backend das camadas estáticas

```text
Execute somente ECO-2306 — backend das camadas Rota e Cidade para origens fixas.

Você é coordenador. Siga integralmente docs/mapa_dinamico/protocolo_sessoes.md. Crie
sequencialmente e encerre um antes do seguinte: planejador → implementador → testador
→ revisor → consolidador. Nenhum subagente cria netos.

Pré-condições: ADR 0011 aceito, ECO-2303 comprovada e ambiente test identificado. Se
qualquer decisão espacial continuar aberta, pare antes de editar.

Objetivo observável:
Implementar vínculo territorial e consultas PostGIS aceitos para retornar pins de
`route_corridor`, `citywide_essential` e `both`, com filtros, contagens e bounds
separados, usando somente dados normalizados existentes.

Leia AGENTS, spec, ADR 0011, contrato Pindobal, OpenAPI, playbook, migrations/modelos,
repository/service territorial, índices espaciais e estratégia de testes Supabase.

Escopo:
- migration oficial somente se exigida pelo ADR;
- modelos/repository/service/API do mapa;
- filtros de categoria/camada/origem;
- índices e consultas espaciais proporcionais;
- testes positivos, negativos, bounds, contagens e desempenho local/test.

Fora do escopo:
- frontend, clustering visual, GPS ou preview dinâmico;
- Google/OSRM em runtime;
- promover migration a staging/production;
- SQL direto em rota FastAPI ou bypass de RLS/grants;
- usar CSV em runtime.

Aceite/testes:
- pins de rota respeitam corredor aceito;
- essenciais vêm da região mesmo fora do corredor;
- route_bounds não é expandido por pin citywide;
- origem fixa continua filtrando corretamente;
- payload tem limite determinístico e query indexada;
- migration list/advisors/testes SQL autorizados, pytest/ruff/mypy/OpenAPI.

Se test remoto não estiver autorizado, registre `PARTIAL`. Não inicie ECO-2307.
```
