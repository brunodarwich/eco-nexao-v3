# Prompt — ECO-2305: decisão de camadas espaciais

```text
Execute somente ECO-2305 — decisão documental das camadas Rota e Cidade.

Você é coordenador. Siga docs/mapa_dinamico/protocolo_sessoes.md e crie em sequência,
um por vez: planejador → implementador documental → testador documental → revisor →
consolidador. Nenhum subagente cria outros.

Objetivo observável:
Produzir proposta para aprovação humana que defina `route_corridor`,
`citywide_essential` e `both`, raios/limites, relação ator–região, route_bounds versus
content_bounds, modos de câmera e limite inicial de pins. Não alterar produto.

Leia AGENTS, spec, contrato Pindobal, ADR 0003/0010, modelos Region/Actor/RouteActor,
consultas PostGIS atuais, critérios AC-MAP e documentos deste pacote.

Permitido:
- criar `docs/adr/0011-camadas-espaciais-do-mapa.md` com status `proposto`;
- comparar `actors.region_id`, tabela de vínculo e alternativas espaciais;
- apresentar amostras/queries read-only ou fixtures para informar decisão;
- atualizar perguntas e matriz de impacto documental.

Proibido:
- inventar raio definitivo, limite municipal ou categoria de camada;
- marcar ADR aceito;
- criar migration, SQL de produção, endpoint ou UI;
- usar somente texto de cidade como solução definitiva sem analisar integridade.

Aceite:
- proposta explica por que serviços da cidade não devem alterar route_bounds;
- define como ator pertence à região e como isso será versionado/testado;
- raios são parâmetros editoriais calibráveis, não números mágicos;
- clustering versus limite/viewport é comparado sem instalar biblioteca;
- owner recebe decisões objetivas e consequências.

Sem aceite explícito, finalize `BLOCKED` e não inicie ECO-2306.
```
