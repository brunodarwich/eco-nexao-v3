{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd econexao-app && npm install && npm run export:web",
  "outputDirectory": "econexao-app/dist",
  "cleanUrls": true,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
